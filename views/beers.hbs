<section class='beers'>
  <ul>
    {{#each beers}}
     <img src='{{this.image_url}}' />
    <li> {{this.name}} </li>
      <li> {{this.description}} </li>
      <li> {{this.tagline}} </li>

    {{/each}}
  </ul>
</section>